package com.pfe.saas.enums;

/**
 * Status of a friendship relationship between two users.
 */
public enum FriendshipStatus {
    ACCEPTED,  // Active friendship
    BLOCKED    // One user has blocked the other (future feature)
}
